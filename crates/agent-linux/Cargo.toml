[package]
name = "tether-agent-linux"
description = "Linux agent. systemd USER unit, unprivileged, dies with the session (HR-7.2)."
edition.workspace = true
version.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
tether-agent-core = { path = "../agent-core" }
tether-proto = { path = "../proto" }
